For installing the requirements run:
pip install -r requirements.txt

For running the model
run:
ablation_study.py --eta --data --fairness_layers --acc_layers --fairness_position --device --epochs --fairness_learning_rate --network_learning_rate --batch_size

For running the model with distraction module run:
UCI Adult dataset:
attribute_classifier.py

Heritage Health dataset:
attribute_classifier_health.py